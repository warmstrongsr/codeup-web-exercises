"use strict"
console.log("This is my external file")
//alert("Welcome to website")
var favColor= prompt("What is your favorite color?")
alert("Great " + favColor + " is my favorite color too!")
console.log(favColor)
console.log(confirm("Click for yes"))
var sample = "Hello Codeup";
sample = +"hello Codeup " + "students";
sample.replace('students', 'class')
sample = "Hello Coddeup " + "students";
sample.replace('students', 'class');
sample.indexOf('c')
sample.indexOf('C');
sample.substring(6, 12);
sample.substring(sample.indexOf('C'), 12)
var theLittleMermaid;
var brotherDays;
var hDays;
const movPrice = 3;
movPrice
theLittleMermaid = 3;
brotherDays = 5;
hDays = 1;
var total = (brotherDays * movPrice) + (theLittleMermaid * movPrice) + (hDays * movPrice)
var total = (brotherDays * movPrice) + (theLittleMermaid * movPrice) + (hDays * movPrice)
var google;
var amazon;
var facebook;
var aHours;
var fHours;
var gHours;
google = 400;
amazon = 380;
facebook = 350;
fHours = 10
gHours = 6;
aHours = 4;
var total = (google * gHours) + (facebook * fHours) + (amazon * aHours);
total
var isFull = false;
var isConflict = false;
let enroll = (!isFull && !isConflict);
var Prem
var offer;
var prem;
var prem = false;
offer = item > 2;
offer = item>2;
var item
offer = item > 2;
var username = 'codeup';
var password = 'notastrongpassword';
var isPwdLong = password.length >= 5;
var isPwdUnique = username != password.indexOf(username) == -1
isPwdUnique
password = username + "extra"
var isTooLong = username.length <= 20
var isTooLong = username.length > 20
sample.substring(sample.indexOf('C'), 12)
//homework is stacked to include re work of previous lesson per instructions